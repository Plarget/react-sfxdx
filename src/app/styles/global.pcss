body {
  font-family: 'Inter', sans-serif;
  overflow-x: hidden;
}

html, body, #root {
  height: 100%;
}

#root {
  display: flex;
  flex-direction: column;
}

img {
  max-width: 100%;
  height: auto;
}

hr {
  border: initial;
}

input:focus {
  outline: none;
}